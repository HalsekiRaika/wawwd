# WAWWD API Server

Required 
* Docker
* Docker-Compose
* rustup 1.72+

Run On
```shell
docker-compose up -d
```

Run Dev
```shell
docker-compose -f docker-compose-test.yml up -d
```

```shell
cargo run -- --debug-assertion
```
